<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bes2.background" >

    <uses-sdk android:minSdkVersion="24" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Android 13 (API 33) 이상에서 알림을 게시하기 위한 권한 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <service
            android:name="com.bes2.background.service.MediaDetectionService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!--
        WorkManager가 백그라운드에서 포그라운드 서비스를 시작할 수 있도록 서비스 선언 추가
        Android 12 (S) 이상에서 ForegroundServiceStartNotAllowedException 방지
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="replace" />
    </application>

</manifest>